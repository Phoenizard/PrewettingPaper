# Reference sweep (242MB of PNGs) stays local; server uses result_cases.txt instead
result/

# Colleague's reference implementation (local study only; not ours, not shared)
reference/

# Generated verification outputs / diagnostics
out/

# Run logs (alongside out/) and throwaway/diagnostic scratch
log/
tmp/

# Python / OS cruft
__pycache__/
*.pyc
.DS_Store

# Session scratch
scratchpad/

# Local Claude Code config + skills (machine-local, not shared)
.claude/

# Local workflow memo (not shared/tracked)
doc/note/Workflow.md

# Local results database + generated gallery (machine-local; later on external HDD)
database/
