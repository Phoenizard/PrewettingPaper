# AGENTS.md

本文件是本仓库对 Codex/ChatGPT 的唯一工作指令。`CLAUDE.md` 已废弃；历史分析与状态以
`PROGRESS.md` 为准，模型与求解条件以 `doc/note/project_plan.md` 为准。

## 当前目标

- 仅完善论文中的 T-a `(chi12, chi13, chi23) = (0, 2.8, 0)` 与 T-f
  `(-8.5, 0, 0)` 两部分。
- 既有物理结论不变。T-a 只补足展示 re-entry 所需的数据；T-f 不再运行新的数值实验。
- 主要工作是出版级制图、图注与正文润色。不得自行扩展到其余四个 stage 或提出新的物理问题。
- 对称性复算、T-f profile 非单调峰收敛核验以及通用数值稳健性测试均已取消。最后一项仅可在
  全文完成后，经用户另行批准，作为可能的 appendix 工作重新提出。

## 计算与文件边界

- 所有数值计算只能在用户指定的 AutoDL 服务器上进行。本地只用于论文文档的归档、整理与
  最终 LaTeX 编辑。
- 共享输入数据只读：`/root/autodl-fs/pw-space/data`。
- 服务器上的仓库、环境、日志、缓存和全部新产物都必须位于 `/root/autodl-tmp/`；当前工作副本为
  `/root/autodl-tmp/PrewettingPaper`。
- 禁止向 `AutoDL-FS` 回写结果。禁止在本地运行模型扫描或求解器。
- 每次实验必须在启动前向用户报告并获得明确确认，报告至少包括：目的、输入 case、参数网格、
  数值精度、并行度、预计产物。一次确认只覆盖所列实验，不得自动扩展扫描范围。
- 每次实验在 `runs/<experiment-id>/` 保存 `manifest.yaml`、输入文件 SHA-256 清单、完整日志、原始
  输出和结果摘要。SHA-256 仅用于确认所用数据版本，并不复制或加密数据。

## 模型与数据约定

- Python 环境名固定为 `numenv`，由仓库的 `environment.yml` 创建；运行形式为
  `conda run -n numenv python <script>`。
- 保持 plain scripts + `src/` modules 的结构，不把项目改造成可安装 Python package。
- `pw_line.csv` 的 `source` 仅表示扫描方向，不是物理 branch；作图和分析时合并处理。
- T-a 的 extent 使用既定定义：MST 剪除大于 `gap_tol=0.015` 的桥边后求长度；到 binodal 的
  距离主报 `dist_mean`，`dist_min` 仅作辅助。不得临时更改定义以改善图形。
- T-f 的溶质交换对称性来自模型，不做数值复算；自然控制坐标为 `omega1 + omega2`。现有数据只
  支持“近似常数和的窄带”这一限定表述，不宣称已经测得完整二维边界。

## 论文与制图约定

- 主图按最终栏宽设计并在最终尺寸检查：刻度不小于 8.5 pt，轴标签约 9.5--10 pt，图例不小于
  8.5 pt，线宽约 1.3--1.8 pt，标记约 3.5--5 pt；每行最多四个 panel。
- 主图同时输出矢量 PDF 与 600 dpi PNG。标题、图注和图内文字不得包含调试信息或文件路径；
  “未采样”与“采样但无 pre-wetting”必须使用不同视觉编码。
- `manuscript/` 是独立的 Overleaf Git 仓库，外层仓库不跟踪。正文结论不得因排版修改而改变。
- Markdown 不使用粗体。LaTeX 公式中不放中文文字，不使用手工间距或尺寸宏。

## 当前获批工作顺序

1. 建立服务器工作区、环境与可复现实验记录。
2. 整理 T-a 现有图并准备两项候选补算；每项仍须在运行前单独获得用户确认。
3. 仅用现有数据重制 T-f 图。
4. 润色 T-a/T-f 正文与图注，编译并按最终版面检查。

