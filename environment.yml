# Recreate the project env on any platform:  conda env create -f environment.yml
# (name matches the local `numenv` used in CLAUDE.md; no build pins for portability)
name: numenv
channels:
  - conda-forge
dependencies:
  - python>=3.10
  - numpy
  - scipy
  - matplotlib
